class Utils {
  /// Checks if string is email.
  static bool isEmail(String email) => true;

  /// Checks if string is phone number.
  static bool isPhoneNumber(String email) => true;

  /// Checks if string is URL.
  static bool isURL(String url) => true;
}
